apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: wildtrace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
    spec:
      containers:
        - name: analytics-service
          image: ghcr.io/your-org/analytics-service:latest
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: wildtrace
spec:
  selector:
    app: analytics-service
  ports:
    - port: 80
      targetPort: 8080
