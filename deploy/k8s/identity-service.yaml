apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-service
  namespace: wildtrace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: identity-service
  template:
    metadata:
      labels:
        app: identity-service
    spec:
      containers:
        - name: identity-service
          image: ghcr.io/your-org/identity-service:latest
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: wildtrace
spec:
  selector:
    app: identity-service
  ports:
    - port: 80
      targetPort: 8080
