apiVersion: apps/v1
kind: Deployment
metadata:
  name: lot-service
  namespace: wildtrace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lot-service
  template:
    metadata:
      labels:
        app: lot-service
    spec:
      containers:
        - name: lot-service
          image: ghcr.io/your-org/lot-service:latest
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: lot-service
  namespace: wildtrace
spec:
  selector:
    app: lot-service
  ports:
    - port: 80
      targetPort: 8080
